require 'spec_helper'

describe EventActions::<%= @class_name%> do
  let(:actor) {}
  let(:subject) {}
  let(:action) {EventActions::<%= @class_name%>.new({}, actor, subject)}

  it "performs action" do
    action.perform
    pending "add test here #{__FILE__}"
  end

end
